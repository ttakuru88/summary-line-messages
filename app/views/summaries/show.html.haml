%h2 結果

%h3 合計

%table
  %tr
    %th 発言数
    %th スタンプ
    %th 写真
    %th 草www

  %tr
    %td= @summary.messages_count
    %td= @summary.stamps_count
    %td= @summary.messages_count
    %td= @summary.kusas_count

%h4 時間帯別

%table

  - 4.times do |ah|
    %tr
      - (ah * 6).upto((ah + 1) * 6 - 1) do |h|
        %th #{h}時
    %tr
      - (ah * 6).upto((ah + 1) * 6 - 1) do |h|
        %td= @summary.message_count_per_hour[h]

-# puts "時間帯別"
-# puts_each_hours(sum[:count], sum[:hours])
-# puts
-# puts "個人別"
-# puts
-# sorted_data.each do |data|
-#   puts "#{data[:name]} さん"
-#   puts "発言数 #{data[:count]}"
-#   puts "スタンプ #{data[:stamps]}"
-#   puts "写真 #{data[:photos]}"
-#   puts "草 #{data[:kusa]}"
-#   puts_each_hours(data[:count], data[:hours])
-#   # p data
-#   puts
-# end
